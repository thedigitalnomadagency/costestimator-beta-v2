const INTIAL_STATE = {
  travelMode: 'DRIVING',
  minDistance: 0,
  config: [],
};

export default (state = INTIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
